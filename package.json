{
  "name": "laurelpilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LaurelPilot: AI film festival discovery, deadline tracking, and submission intelligence.",
  "scripts": {
    "start": "node server/static.mjs",
    "check": "node --check public/logic.js && node --check public/app.js && node --check public/festivals.js && node --check server/static.mjs",
    "test": "node --test tests/*.test.mjs",
    "no-api-scan": "node scripts/no-api-scan.mjs",
    "ci": "npm run check && npm test && npm run no-api-scan",
    "build": "npm run ci"
  },
  "engines": {
    "node": ">=20"
  }
}
